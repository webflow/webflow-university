---
'scripts': patch
---

Slack YouTube embed alerts for blocked viewers (ad blocker, VPN, firewall, CSP) and YouTube onError, distinguished by payload `failureKind` (`viewer-blocked`, `youtube-player`, `qa-force`). Skip bingbot user agents.

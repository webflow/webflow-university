---
"scripts": patch
---

Patch dependency resolutions for Socket.dev SCA advisories (APPSEC-2107 / 2167 / 2199 / 2213 / 2252) by raising workspace `pnpm` overrides for `vitest`, `picomatch`, `brace-expansion`, and `postcss`, and syncing workspace `vitest` specs.
